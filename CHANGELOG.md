# Changelog

早期版本 (v0.1 ~ v0.6) 及完整历史见 [GitHub Releases](https://github.com/shimmerjordan/home_repo_git/releases)。

## Unreleased

### 三个提醒列表不再无限往下铺

「待补充」「待归位」「近期取放记录」以前都是一次全铺开, 15 件待补充就把页面拽长
一屏。现在各自限高可滚, 并带分页 (每页 10/20/50 可选, 按区域分别记住)。

- 近期取放记录走**服务端**分页 (`/api/transactions` 新增 `offset`): 这一段每 30s
  轮询一次, 一次只拉当前页那几条 —— 换成前端切几百条的话, 翻得越深轮询越贵
- 排序补了 id 作次序键: 一次批量操作写进的几条流水 created_at 完全相同, 只按时间
  排的话翻页会重复/漏行

### 位置图标不再一律回落到 📍

位置树、文件夹视图各自抄了一份只有五六种的图标表, 于是冰箱、衣柜、书桌、洗衣机在
位置页统统显示成 📍。现在三处都从家具目录取同一份 (`iconFor`), 24 种各有各的图标;
类型下拉也由目录派生, 从 14 种补到 24 种 —— 以前 3D 页能建冰箱, 位置页却改不成冰箱。

### 2D 平面图能按层看了

俯视图里"第几层"只影响高度、不影响平面位置, 所以一个 3 层柜子每层放 4 个收纳箱,
在 2D 上就是 4 摞、每摞 3 个精确重叠: 点不中、拖不动、标签糊成一团。

- 选中多层容器 (或它里面的东西) 后出现层筛选条, 每层标着子级数量
- 只看某一层时, 其余层压成底图: 不画标签, 也不接收点击 —— 拖动不会再抓错层
- 「本层排开」把当前层的东西沿父级宽度依次排开, 一次操作只占一步撤销
- 正盯着某一层时放新家具, 直接落在这一层, 不再多问一句"放第几层"
- 外部标签横向撞车时自动错行, 并加了白描边压在图形上也读得清


## v0.10.1 NAS 部署的三处硌脚

都是 v0.10.0 发出去之后在 QNAP 上实装时踩出来的, 不涉及功能。

- **NAS 版 compose 去掉了 whisper 服务**: Container Station 不认 compose 的 `profiles`,
  即使没激活也照样去 Docker Hub 拉, 而 `registry-1.docker.io` 常年超时, 整个应用会
  因为这个可选服务创建失败。语音默认走浏览器自带识别 (`whisper_enabled` 默认关),
  不装它一样能用
- **NAS 版只映射 HTTPS 一个端口**: 项目默认的 HTTP 口 8080 在 QNAP 上必撞 QTS 管理
  界面 (8081 也被自带 Web 服务器占着)。证书在设置页里就能下, `/ca.crt` 走 HTTPS
  一样能装, 少开一个口就少一次冲突
- **设置页不再写死端口号**: 原来固定显示 `8080` / `8443`, 而端口本来就是可配的,
  凡是改过端口的人看到的说明都是错的。现在从当前访问地址现取

## v0.10.0 说不清就先问一句

这一版的主题是**让 AI 少猜**。以前它拿不准也照做, 错了你得自己发现; 现在拿不准就
先问, 高风险的动作必须你点头才落库。

### 群机器人: 高风险操作先出方案等确认

钉钉 / Telegram / 飞书以前是"说完就执行", 三个文件各抄了一份处理逻辑 (包括那句把
置信度强抬到 1.0 的)。现在收成一份公共流程:

- **删档 / 没认准是哪件东西 / 一次改 3 条以上**, 先回一份方案, 你回"确认"才执行,
  回"取消"就作废, 5 分钟不理会自动失效
- 位置说不准时直接问是哪一个, 不再走确认流程 —— 二选一的"确认"解不了三选一的位置
- 待确认方案按 (渠道, 会话, 发送者) 归属, 认不出说话人时**拒绝**高风险操作,
  而不是记到一个全群共用的槽位上
- 三份消息渲染收成一份纯文本 (钉钉那份 markdown 表格随之取消)

### AI 准确性

- **位置名歧义不再猜**: 以前说"书桌"而库里有"书桌1"和"书桌10"时会静默落到其中一个,
  现在把候选列出来让你挑
- **存入找不到的物品不再静默建档**, 改成问"库里没有 X, 要新建吗"
- 多物品句里的"新增"只管它所在的那个分句 ——「新增跑步机到书房, 再放两个螺丝到工具箱」
  不会再给已有的螺丝建一条重复档案
- 认识量词 (一打 = 12) 和否定句 (「拿卷尺, 别拿螺丝刀」只拿卷尺)
- 库存增减逻辑从三处收敛成一份

### 性能与功耗

- 3D 场景改按需渲染: 空闲彻底停帧, 三重门禁 (页面可见 / 容器在视口 / keep-alive 未换出)
- 面板从 `v-show` 全量挂载改 `v-if + keep-alive` 懒挂载; 诊断页 3s / 语音页 30s 轮询绑定可见性
- 新增 `useInventoryStore` 共享物品/位置数据 (in-flight 去重 + generation 防脏读),
  开机重复请求从 items×3 + locations×4 降到各 1 次
- nginx 开 gzip + `/assets/` 永久缓存 (Scene3D chunk 580KB 此前裸传)
- 后端: `transactions` 加 `item_id`/`location_id` 索引; `pending-returns` 去 N+1;
  `intent.py` 的 Location/Item 全表查询按 session 缓存 (flush 即失效)
- telegram / 飞书 / 备份三个后台循环关闭态零唤醒 (挂 `asyncio.Event`, 由 `reload()` 唤醒)

### 修复

- **负数数量会把库存改成反的**: 模型偶尔把"少了两个"解析成 -2, 而"取出 -2"
  经 `max(0, q-(-2))` 变成给库存**加** 2。现在钳到 1
- **说了"别拿"却照样拿**: 否定过滤算出了正确结果, 却被顶层字段那条回退路径
  重建出来照常执行了
- 机器人问"要新建吗"却没存待确认, 用户回"确认"只会得到"没有待确认的操作"
- LLM 报错原文 (可能含 API key 前缀、内网地址) 不再随回复发进群
- Telegram 匿名发送者的身份不再塌缩成字符串 `"None"` 骗过身份守卫
- 群里 `@机器人 确认` 现在有效, 且只认机器人自己的用户名 (以前剥任意 `@某人`,
  一句说给别人听的"@张三 确认"会触发自己挂起的删档)
- `#tab=backup` 刷新会掉回语音页 (`VALID_TABS` 漏了 `backup`)

### 证书与部署

- 证书 SAN 现在也收**主机名** (`LAN_HOSTS=nas.local`), 不只是 IP —— 局域网 IP 是
  DHCP 发的会变, mDNS 名字不会。填不进去的条目会明确报出来而不是静默丢弃

### 内部

- 后端测试 76 → 170; AI 准确度评测 32 → 47 条, 新增落库断言与 xfail 机制
- 评测台打分接上位置期望 (以前 13 条 case 的位置期望是死字段, 位置解析整个失效
  也只有 2 条会红)
- 文档与 CI 瘦身: README/部署文档去重, architecture 结构树换职责表
- 清理前后端零引用代码与重复定义

用户可见变化: 群机器人对高风险操作会先问一句; `#tab=backup` 刷新后停在备份页;
数据无变化的刷新不再重置 3D 相机; 飞书开启态改设置立即生效, 不用再等最长 15s。

## v0.8 可信任的 AI + 单容器

- 落库前逐条确认: AI 只出方案 (stage=plan), 每个单品一个候选下拉 + 「新建, 名字自己填」+ 位置 + 跳过; 确认后 `/api/voice/apply` 单事务执行
- 模糊命中不再自动加库存: 说"存入洗发水"默认新建, 不再悄悄记到"洗手液"头上
- 「新增 X 到 Y」= 全新物品, 不做同名合并 (想合并在下拉里挑已有物品)
- 回撤按钮进「近期取放记录」与「流水」页; undoable 服务端算, 撤不了的按钮置灰并说明原因
- AI 准确度: max_tokens 512→4096 + 截断检测自动重试 (多物品语句不准的头号原因); 按物品分段检索; 准确度评测台 `backend/eval/` (32 条标注语句, cassette 录制/回放可离线复现)
- 飞书长连接卡死修复: 收回重连主权, 判死看连接而不是看线程, 连接数超限走长退避
- 前后端合并成单容器 `storage-app` (nginx + uvicorn by supervisord), 端口/数据布局不变
- GitHub 发布流水线: 打 v* tag → 测试闸门 → 构建 amd64+arm64 推 GHCR → 建 Release, 附版本钉死的 compose.yml

## v0.7 WebDAV 数据备份

- 全量备份到任意 WebDAV (坚果云/Nextcloud/群晖...): 设置 + 物品 + 位置 + 流水 + 审计 + 日志
- 选择性备份 (按组件勾选) + 裸 SQLite 快照为恢复主道
- GFS 分层保留: 日/周/月各保留最近 N 个, 自动清理过期备份
- 定时自动备份 (hourly/daily/weekly) + 手动「立即备份」, 改配置即时生效
- AES-256 (PBKDF2 + GCM) 口令加密, 可选
- 恢复: 从远程备份点或上传包还原, 恢复前自动本地快照
- 新增独立「☁ 备份」标签页

---

完整版本历史 (含 v0.1 ~ v0.6) 见 [GitHub Releases](https://github.com/shimmerjordan/home_repo_git/releases) 与 git 历史。
